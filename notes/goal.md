### Overall goal
Train a decoder-only transformer (task = text-generation) to generate recipes for a dish name entered by the user. The dish name will likely be embedded into a larger (more detailed prompt).

### Steps
1. Pretraining on other various English language documents.
2. Fine-tuning on the recipe corpus.